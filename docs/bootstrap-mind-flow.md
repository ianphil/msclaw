# Bootstrap Flow: `--mind /path`

```
msclaw --mind /path/to/mind
         │
         ▼
┌─────────────────────┐
│ BootstrapOrchestrator│
│   .Run(args)        │
└────────┬────────────┘
         │ parse CLI args
         ▼
┌─────────────────────┐
│ Path.GetFullPath()  │  resolve to absolute
└────────┬────────────┘
         ▼
┌─────────────────────┐
│ MindValidator       │
│   .Validate(path)   │
└────────┬────────────┘
         │
    ┌────┴────┐
    │         │
  valid    invalid
    │         │
    │         ▼
    │    ┌──────────────┐
    │    │ stderr:      │
    │    │  • errors[]  │
    │    │ throw        │
    │    │ Exit(1)      │
    │    └──────────────┘
    ▼
┌─────────────────────┐
│ ConfigPersistence   │
│   .Save(config)     │  ~/.msclaw/config.json
└────────┬────────────┘
         ▼
┌─────────────────────┐
│ BootstrapResult     │
│  MindRoot: /path    │
│  IsNewMind: false   │
│  HasBootstrapMarker:│
│    bootstrap.md?    │
└────────┬────────────┘
         ▼
┌─────────────────────┐
│ Program.cs          │
│  Override MindRoot  │
│  Register services  │
│  Build + Run Kestrel│
└────────┬────────────┘
         ▼
    ┌────┴────────┐
    │  /chat      │
    │  request    │
    └────┬────────┘
         │
    ┌────┴────┐
    │         │
 bootstrap  no bootstrap
 .md exists  .md
    │         │
    ▼         ▼
 prepend    normal
 bootstrap  IdentityLoader
 instruc-   SOUL.md +
 tions to   agents/*.md
 system     → system msg
 message
    │         │
    └────┬────┘
         ▼
   Copilot SDK
   session response
```
